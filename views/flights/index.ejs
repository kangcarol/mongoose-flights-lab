<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/flights/index.css" />
<%- include('../partials/nav') %>

<main>
  <h1>Flight List</h1>
  <table id="list">
    <thead>
      <tr>
        <th>Airline</th>
        <th>Airport</th>
        <th>Flight No.</th>
        <th>Departs</th>
        <th>Show</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      <% flights.forEach(flight => { %>
        <tr>
          <td><%= flight.airline %></td>
          <td><%= flight.airport %></td>
          <td><%= flight.flightNo %></a></td>
          <td><%= (flight.departs).toLocaleString() %></td>
          <td>
            <form
              action="/flights/<%= flight._id %>"
              class="edit-form"
              method="GET"
            >
            <button class="btn btn-outline-light btn-sm index-btns" type="submit">detail</button>
            </form>
          </td>
          <td>
            <form
              action="/flights/<%= flight._id %>?_method=DELETE"
              class="delete-form"
              method="POST"
            >
            <button class="btn btn-outline-light btn-sm index-btns" type="submit">delete</button>
            </form>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>
</main>

<%- include('../partials/footer') %>
